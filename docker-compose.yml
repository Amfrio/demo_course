version: '3.8'

services:
  bot:
    build: .
    container_name: meditation_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./static:/app/static
      - ./templates:/app/templates
    networks:
      - meditation_network
    depends_on:
      - webapp
    command: ["python", "main.py"]

  webapp:
    build: .
    container_name: meditation_webapp
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "8000"
    volumes:
      - ./data:/app/data
      - ./static:/app/static
      - ./templates:/app/templates
    networks:
      - meditation_network
    command: ["python", "-m", "uvicorn", "webapp.app:app", "--host", "0.0.0.0", "--port", "8000"]

  nginx:
    image: nginx:alpine
    container_name: meditation_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./static:/usr/share/nginx/html/static:ro
      # ⚠️ Укажи правильные пути к сертификату и ключу на сервере:
      - /etc/ssl/maksim-shikhov.ru.crt:/etc/ssl/maksim-shikhov.ru.crt:ro
      - /etc/ssl/maksim-shikhov.ru.key:/etc/ssl/maksim-shikhov.ru.key:ro
    networks:
      - meditation_network
    depends_on:
      - webapp

networks:
  meditation_network:
    driver: bridge

volumes:
  data:
